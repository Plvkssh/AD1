package service;

import model.User;
import org.junit.Before;
import org.junit.Test;
import static org.junit.Assert.*;

public class ShorteningServiceTest {
    private ShorteningService service;
    private User testUser;

    @Before
    public void setup() {
        service = new ShorteningService();
        testUser = new User("testUser");
    }

    @Test
    public void testShortenUrl() {
        String shortUrl = service.shortenUrl("https://example.com", testUser, 5);
        assertNotNull(shortUrl);
    }

    @Test
    public void testUpdateClickLimit() throws Exception {
        String shortUrl = service.shortenUrl("https://example.com", testUser, 5);
        service.updateClickLimit(shortUrl, 10, testUser);
        // Проверка нового лимита
    }

    @Test
    public void testCleanExpiredLinks() {
        // Добавьте логику проверки устаревших ссылок
    }
}
